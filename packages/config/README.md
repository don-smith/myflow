# @myflow/config

Internal shared configuration helpers used by MyFlow's optional Pi extensions. It stores per-extension configuration under `~/.myflow/config/<feature>/config.json` and fails safely when configuration is absent or malformed.
