# Architecture flows

## Request flow

[model:flow-request] traces [model:el-system] calling [model:el-module] through [model:rel-calls]. Replace this example with important startup, success, failure, recovery, persistence, or replication sequences.
